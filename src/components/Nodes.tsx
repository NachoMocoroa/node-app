import React from 'react';
import Logout from './Logout';

const Nodes = () => {
    
    return (
        <div>
            <h1>Nodes</h1>
            <Logout />
        </div>
    );
};

export default Nodes;
